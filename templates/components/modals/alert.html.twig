<div class="modal {{ app.session.flashBag.has('alert.message') ? 'active' : '' }}" id="alert-modal">
  <div class="modal-content">
    <div class="alert-body">
      <p class="message">{{ app.session.flashBag.get('alert.message') | last }}На ваш лалалал лаовла ловаыд</p>
    </div>
  </div>
</div>
