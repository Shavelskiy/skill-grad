<div class="breadcrumbs">
  <ul>
    <li><a href="{{ path('site.index') }}">Главная</a></li>
    {% if additionalBreadcrumb is defined %}
      <li><a href="{{ additionalBreadcrumb.link }}">{{ additionalBreadcrumb.name }}</a></li>
    {% endif %}
    <li><a class="active" href="#">{{ title }}</a></li>
  </ul>
</div>
